# version: '3'
services:
  frontend:
    image: ahmedhanzala01/frontend-app:v2  # Updated to v2
    ports:
      - "8080:80"

  backend:
    image: ahmedhanzala01/backend-app:v1  # Use the Docker Hub image for backend
    ports:
      - "3000:3000"
